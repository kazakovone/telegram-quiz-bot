import os
from collections import Counter

from telegram import (
    Update,
    InlineKeyboardMarkup,
    InlineKeyboardButton,
)
from telegram.constants import ParseMode
from telegram.ext import (
    ApplicationBuilder,
    CommandHandler,
    CallbackQueryHandler,
    ContextTypes,
)

TEMPLATES_URL = "https://vigbo.com/templates"
CABINET_URL = "https://clients.vigbo.com/area/main.php"
JULIA_URL = "https://t.me/julia_vigbo"

# ---------------------- ВОПРОСЫ ----------------------

QUESTIONS = [
    {
        "title": "Вопрос 1 из 15",
        "question": "Что для тебя самое важное в фотографии?",
        "options": [
            "1. Идея и смысл кадра",
            "2. Настоящие чувства и доверие",
            "3. Качественная продающая съёмка",
            "4. Тишина, свет и движение",
            "5. История, которую проживают",
        ],
    },
    {
        "title": "Вопрос 2 из 15",
        "question": "Когда ты снимаешь, ты чаще думаешь о…",
        "options": [
            "1. Цвете, композиции и свете",
            "2. Настроении и атмосфере",
            "3. Качестве картинки и технике",
            "4. Взаимодействии пространства",
            "5. Истории серии и последовательности",
        ],
    },
    {
        "title": "Вопрос 3 из 15",
        "question": "Что тебе ближе в работе с клиентом?",
        "options": [
            "1. Вдохновить и предложить идею",
            "2. Поддержать и помочь раскрыться",
            "3. Чётко объяснить процесс",
            "4. Наблюдать и ловить естественность",
            "5. Показать человека как героя",
        ],
    },
    {
        "title": "Вопрос 4 из 15",
        "question": "Как ты отбираешь фото в портфолио?",
        "options": [
            "1. Передаёт мою идею",
            "2. Сильные эмоции",
            "3. Технически идеально",
            "4. Воздух и честность момента",
            "5. Складываются в рассказ",
        ],
    },
    {
        "title": "Вопрос 5 из 15",
        "question": "Что для тебя «идеальный сайт»?",
        "options": [
            "1. Арт-пространство",
            "2. Уютный разговор",
            "3. Понятный инструмент",
            "4. Минималистичная галерея",
            "5. Как фильм или книга",
        ],
    },
    {
        "title": "Вопрос 6 из 15",
        "question": "Что ты чувствуешь, когда выкладываешь снимки?",
        "options": [
            "1. Лёгкое волнение",
            "2. Радость делиться",
            "3. Гордость за результат",
            "4. Тихое удовлетворение",
            "5. Интерес к реакции людей",
        ],
    },
    {
        "title": "Вопрос 7 из 15",
        "question": "Как ты относишься к трендам?",
        "options": [
            "1. Вдохновляюсь, но по-своему",
            "2. Беру идеи, если близко",
            "3. Следую тем, что приносят клиентов",
            "4. Наблюдаю, не повторяю",
            "5. За timeless-стиль",
        ],
    },
    {
        "title": "Вопрос 8 из 15",
        "question": "Что для тебя сложнее всего?",
        "options": [
            "1. Отпустить идею",
            "2. Говорить о себе",
            "3. Вдохновение в рутине",
            "4. Работать «на показ»",
            "5. Структурировать материал",
        ],
    },
    {
        "title": "Вопрос 9 из 15",
        "question": "Как описать твою съёмку одним словом?",
        "options": [
            "1. Атмосферная",
            "2. Живая",
            "3. Уверенная",
            "4. Спокойная",
            "5. Цельная",
        ],
    },
    {
        "title": "Вопрос 10 из 15",
        "question": "Как ты принимаешь решения по проектам?",
        "options": [
            "1. По вдохновению",
            "2. По ощущению «моё/не моё»",
            "3. По логике и плану",
            "4. Через тишину и спокойствие",
            "5. Через историю",
        ],
    },
    {
        "title": "Вопрос 11 из 15",
        "question": "Что должны чувствовать люди, глядя на твои фото?",
        "options": [
            "1. Вдохновение",
            "2. Тепло и принятие",
            "3. Уверенность",
            "4. Спокойствие",
            "5. Увлечённость",
        ],
    },
    {
        "title": "Вопрос 12 из 15",
        "question": "Какая фраза ближе тебе?",
        "options": [
            "1. «Фотография — язык света»",
            "2. «Быть рядом и видеть»",
            "3. «Работа говорит сама»",
            "4. «Я наблюдаю, мир делает остальное»",
            "5. «Каждый кадр — фраза истории»",
        ],
    },
    {
        "title": "Вопрос 13 из 15",
        "question": "Когда думаешь о будущем, тебе ближе…",
        "options": [
            "1. Творчество и стиль",
            "2. Аудитория и сообщество",
            "3. Стабильность и заказы",
            "4. Свобода и время для себя",
            "5. Авторская серия и выставки",
        ],
    },
    {
        "title": "Вопрос 14 из 15",
        "question": "Какой комплимент особенно приятен?",
        "options": [
            "1. «Это искусство»",
            "2. «С тобой легко»",
            "3. «Профессионально и чисто»",
            "4. «Фото как медитация»",
            "5. «Как фильм, смотришь не отрываясь»",
        ],
    },
    {
        "title": "Вопрос 15 из 15",
        "question": "Когда ты создаёшь сайт, что важно?",
        "options": [
            "1. Отражение внутреннего мира",
            "2. Живое присутствие",
            "3. Понятная структура",
            "4. Простота и крупные фото",
            "5. Цельная история",
        ],
    },
]

TYPE_NAMES = {
    1: "Творец",
    2: "Эмпат",
    3: "Практик",
    4: "Наблюдатель",
    5: "История",
}

# (Чтобы ответ не раздувать до безумия, тут даю укороченные описания.
# Если захочешь — можем подставить длинные тексты, которые ты давал ранее.)
TYPE_TEXTS = {
    1: (
        "<b>Ты – Фотограф-творец.</b>\n\n"
        "Ты снимаешь, чтобы выразить внутренний мир. "
        "Для тебя важны идея, художественность и атмосфера. "
        "Сайт для тебя — арт-пространство: много воздуха, крупные кадры, минимум шума.\n\n"
        "Подойдут атмосферные, самобытные шаблоны с акцентом на визуал и историю."
    ),
    2: (
        "<b>Ты – Фотограф-эмпат.</b>\n\n"
        "Ты видишь чувства людей и создаёшь пространство доверия. "
        "Главное — тепло, забота и живой контакт. "
        "Сайт для тебя — тёплый и человечный, как разговор с другом, "
        "с историями клиентов и отзывами."
    ),
    3: (
        "<b>Ты – Фотограф-практик.</b>\n\n"
        "Ты любишь понятный процесс, структуру и результат. "
        "Съёмка для тебя — профессиональный сервис. "
        "Сайт должен быть чётким, логичным, с пакетами услуг и быстрым путём к заявке."
    ),
    4: (
        "<b>Ты – Фотограф-наблюдатель.</b>\n\n"
        "Ты тонко чувствуешь момент, свет и детали. "
        "Любишь тишину и естественность. "
        "Сайт для тебя — минималистичное пространство с большим воздухом и крупными кадрами."
    ),
    5: (
        "<b>Ты – Фотограф-история.</b>\n\n"
        "Ты видишь жизнь как рассказ, а серии — как главы. "
        "Для тебя важен ритм и целостность. "
        "Сайт должен ощущаться как фильм: логичный поток блоков, серии-истории и тексты-сценарии."
    ),
}


# ---------------------- ВСПОМОГАТЕЛЬНОЕ ----------------------

def build_question_keyboard(options) -> InlineKeyboardMarkup:
    return InlineKeyboardMarkup(
        [[InlineKeyboardButton(text=opt, callback_data=f"ans_{i+1}")]
         for i, opt in enumerate(options)]
    )


def build_after_quiz_keyboard() -> InlineKeyboardMarkup:
    return InlineKeyboardMarkup(
        [
            [InlineKeyboardButton("Узнать мой тип", callback_data="show_type")],
        ]
    )


def build_final_keyboard() -> InlineKeyboardMarkup:
    return InlineKeyboardMarkup(
        [
            [
                InlineKeyboardButton(
                    "Посмотреть шаблоны сайтов от Vigbo",
                    url=TEMPLATES_URL,
                )
            ],
            [
                InlineKeyboardButton(
                    "Перейти в личный кабинет Vigbo",
                    url=CABINET_URL,
                )
            ],
            [
                InlineKeyboardButton(
                    "Пройти тест ещё раз",
                    callback_data="restart_quiz",
                )
            ],
        ]
    )


def build_julia_keyboard() -> InlineKeyboardMarkup:
    return InlineKeyboardMarkup(
        [
            [
                InlineKeyboardButton(
                    "Написать Юле в Telegram",
                    url=JULIA_URL,
                )
            ]
        ]
    )


async def send_question(chat, q_index: int, context: ContextTypes.DEFAULT_TYPE):
    q = QUESTIONS[q_index]
    keyboard = build_question_keyboard(q["options"])
    text = f"<b>{q['title']}</b>\n\n{q['question']}"

    await chat.send_message(
        text=text,
        parse_mode=ParseMode.HTML,
        reply_markup=keyboard,
    )


# ---------------------- ХЕНДЛЕРЫ ----------------------

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Запуск теста командой /start."""
    context.user_data["answers"] = []
    context.user_data["q_index"] = 0
    context.user_data["dominant_types"] = None

    intro = (
        "Привет! Это тест <b>«Какой ты фотограф?»</b>\n\n"
        "Отвечай интуитивно — выбирай вариант, который ближе всего по ощущению.\n"
        "В конце ты узнаешь свой тип и получишь рекомендации по сайту и шаблонам Vigbo."
    )
    await update.message.reply_text(intro, parse_mode=ParseMode.HTML)

    await send_question(update.effective_chat, 0, context)


async def answer_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработка ответов ans_1 … ans_5."""
    query = update.callback_query
    data = query.data

    if not data.startswith("ans_"):
        return

    await query.answer()

    try:
        choice = int(data.split("_")[1])
    except ValueError:
        return

    answers = context.user_data.get("answers", [])
    answers.append(choice)
    context.user_data["answers"] = answers

    q_index = context.user_data.get("q_index", 0) + 1
    context.user_data["q_index"] = q_index

    chat = query.message.chat

    if q_index < len(QUESTIONS):
        await send_question(chat, q_index, context)
    else:
        # все вопросы пройдены — считаем результат и предлагаем кнопку
        counts = Counter(answers)
        for t in range(1, 6):
            counts.setdefault(t, 0)

        max_count = max(counts.values())
        dominant = [t for t, c in counts.items() if c == max_count]
        context.user_data["dominant_types"] = dominant

        text = (
            "Ура! 🎉 Ты прошёл(а) тест, и мы смогли определить твой тип фотографа.\n\n"
            "Нажми кнопку ниже, чтобы узнать свой результат."
        )

        await chat.send_message(
            text=text,
            reply_markup=build_after_quiz_keyboard(),
        )


async def show_type_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Показываем тип(ы) после клика по «Узнать мой тип»."""
    query = update.callback_query
    await query.answer()

    dominant = context.user_data.get("dominant_types") or []

    chat = query.message.chat

    if not dominant:
        await chat.send_message(
            "Похоже, данные теста не найдены. Попробуй пройти его ещё раз с команды /start."
        )
        return

    if len(dominant) == 1:
        t = dominant[0]
        await chat.send_message(
            text=TYPE_TEXTS[t],
            parse_mode=ParseMode.HTML,
        )
    else:
        names = ", ".join(TYPE_NAMES[t] for t in dominant)
        intro = (
            "Похоже, твой типаж включает несколько граней — "
            f"<b>{names}</b>.\n\n"
            "Это абсолютно нормально: фотография редко укладывается в одну коробку. "
            "Как раз сочетание этих типов и создаёт твою уникальность."
        )
        await chat.send_message(intro, parse_mode=ParseMode.HTML)

        for t in dominant:
            await chat.send_message(
                text=TYPE_TEXTS[t],
                parse_mode=ParseMode.HTML,
            )

    # после типов — финальные кнопки Vigbo
    final_text = (
        "Теперь ты лучше понимаешь свой взгляд на съёмку. "
        "Дальше можно перейти к сайту и шаблонам, которые поддержат твой стиль."
    )

    await chat.send_message(
        text=final_text,
        reply_markup=build_final_keyboard(),
        parse_mode=ParseMode.HTML,
    )

    # и отдельное сообщение про Юлю
    julia_text = (
        "Если остались вопросы по созданию сайта, которые хочется обсудить вслух "
        "или просто попросить совет – напиши менеджеру Vigbo Юле. "
        "Она рядом и всегда постарается помочь."
    )

    await chat.send_message(
        text=julia_text,
        reply_markup=build_julia_keyboard(),
        parse_mode=ParseMode.HTML,
    )


async def restart_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Кнопка «Пройти тест ещё раз»."""
    query = update.callback_query
    await query.answer()

    context.user_data["answers"] = []
    context.user_data["q_index"] = 0
    context.user_data["dominant_types"] = None

    await send_question(query.message.chat, 0, context)


# ---------------------- ЗАПУСК ----------------------

def main():
    token = os.getenv("BOT_TOKEN")
    if not token:
        raise RuntimeError("Не найден токен. Установи переменную BOT_TOKEN.")

    app = ApplicationBuilder().token(token).build()

    app.add_handler(CommandHandler("start", start))
    app.add_handler(CallbackQueryHandler(restart_handler, pattern="^restart_quiz$"))
    app.add_handler(CallbackQueryHandler(show_type_handler, pattern="^show_type$"))
    app.add_handler(CallbackQueryHandler(answer_handler, pattern="^ans_"))

    app.run_polling()


if __name__ == "__main__":
    main()
